---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - Makefile
autonomous: true

must_haves:
  truths:
    - "CI pipeline runs tests on every push and PR"
    - "CI pipeline runs clippy linting"
    - "CI pipeline generates code coverage reports"
    - "CI passes when code is correct, fails when tests fail"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI workflow"
      contains: "cargo nextest run"
    - path: ".github/workflows/ci.yml"
      provides: "Coverage reporting"
      contains: "cargo-llvm-cov"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "GitHub Actions"
      via: "workflow trigger"
      pattern: "on:.*push"
---

<objective>
Create CI pipeline with tests, linting, and coverage

Purpose: Establish automated quality gates that run on every commit, ensuring code quality doesn't regress. The CI pipeline validates that tests pass, code follows Rust idioms (clippy), and maintains coverage visibility.

Output: .github/workflows/ci.yml with test, lint, and coverage jobs
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

@Cargo.toml
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create the GitHub Actions workflow for CI:

1. **Create .github/workflows directory** if it doesn't exist

2. **Create .github/workflows/ci.yml**:
   ```yaml
   name: CI

   on:
     push:
       branches: [master, main]
     pull_request:
       branches: [master, main]

   env:
     CARGO_TERM_COLOR: always
     RUST_BACKTRACE: 1

   jobs:
     test:
       name: Test
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4

         - name: Install Rust
           uses: dtolnay/rust-toolchain@stable

         - name: Cache cargo registry
           uses: actions/cache@v4
           with:
             path: |
               ~/.cargo/registry
               ~/.cargo/git
               target
             key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
             restore-keys: |
               ${{ runner.os }}-cargo-

         - name: Install cargo-nextest
           uses: taiki-e/install-action@nextest

         - name: Run tests
           run: cargo nextest run --profile ci

         - name: Run doctests
           run: cargo test --doc

     lint:
       name: Lint
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4

         - name: Install Rust
           uses: dtolnay/rust-toolchain@stable
           with:
             components: clippy, rustfmt

         - name: Cache cargo registry
           uses: actions/cache@v4
           with:
             path: |
               ~/.cargo/registry
               ~/.cargo/git
               target
             key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
             restore-keys: |
               ${{ runner.os }}-cargo-

         - name: Check formatting
           run: cargo fmt -- --check

         - name: Run clippy
           run: cargo clippy -- -D warnings

     coverage:
       name: Coverage
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4

         - name: Install Rust
           uses: dtolnay/rust-toolchain@stable
           with:
             components: llvm-tools-preview

         - name: Cache cargo registry
           uses: actions/cache@v4
           with:
             path: |
               ~/.cargo/registry
               ~/.cargo/git
               target
             key: ${{ runner.os }}-cargo-coverage-${{ hashFiles('**/Cargo.lock') }}
             restore-keys: |
               ${{ runner.os }}-cargo-coverage-

         - name: Install cargo-llvm-cov
           uses: taiki-e/install-action@cargo-llvm-cov

         - name: Install cargo-nextest
           uses: taiki-e/install-action@nextest

         - name: Generate coverage report
           run: cargo llvm-cov nextest --lcov --output-path lcov.info

         - name: Upload coverage to Codecov
           uses: codecov/codecov-action@v4
           with:
             files: lcov.info
             fail_ci_if_error: false  # Don't fail if codecov is not configured
   ```

Key design decisions:
- **Three parallel jobs**: test, lint, coverage run independently for faster CI
- **cargo-nextest**: Uses taiki-e/install-action for reliable installation
- **Caching**: Cargo registry cached to speed up subsequent runs
- **Doctests separate**: nextest doesn't run doctests, so run separately
- **Codecov optional**: fail_ci_if_error: false allows CI to pass without codecov configured
- **clippy -D warnings**: Treats warnings as errors (Phase 2 will clean these up)
  </action>
  <verify>
Verify workflow file syntax:
```bash
cat /home/ms/projects/ci-tui/.github/workflows/ci.yml
```
Verify YAML is valid (basic check):
```bash
cd /home/ms/projects/ci-tui && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>&1 && echo "YAML valid" || echo "YAML invalid"
```
  </verify>
  <done>
- .github/workflows/ci.yml exists
- Has test, lint, and coverage jobs
- Uses cargo-nextest for tests
- Uses cargo-llvm-cov for coverage
- Clippy runs with -D warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Add local CI commands to Makefile</name>
  <files>Makefile</files>
  <action>
Update the Makefile to add commands that mirror CI locally:

1. **Read existing Makefile** to understand current structure

2. **Add new targets** for local CI simulation:
   ```makefile
   # Local CI commands (mirrors GitHub Actions)

   .PHONY: ci
   ci: fmt-check lint test  ## Run all CI checks locally

   .PHONY: test-local
   test-local:  ## Run tests with nextest (local)
   	cargo nextest run

   .PHONY: test-ci
   test-ci:  ## Run tests with nextest (CI profile)
   	cargo nextest run --profile ci

   .PHONY: fmt-check
   fmt-check:  ## Check formatting without fixing
   	cargo fmt -- --check

   .PHONY: coverage
   coverage:  ## Generate HTML coverage report
   	cargo llvm-cov nextest --html --open

   .PHONY: coverage-lcov
   coverage-lcov:  ## Generate LCOV coverage report
   	cargo llvm-cov nextest --lcov --output-path lcov.info
   ```

3. **Keep existing targets** - don't remove or modify the Docker-based targets

These commands let developers run the same checks locally before pushing.
  </action>
  <verify>
Verify Makefile is valid:
```bash
cd /home/ms/projects/ci-tui && make --dry-run ci 2>&1 | head -10 || echo "make target check"
```
List available targets:
```bash
cd /home/ms/projects/ci-tui && make help 2>&1 || grep -E "^[a-z].*:.*##" Makefile | head -10
```
  </verify>
  <done>
- Makefile has ci target that runs fmt-check, lint, test
- Makefile has coverage targets
- Existing Docker-based targets preserved
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. **Workflow exists**: `.github/workflows/ci.yml` exists and is valid YAML
2. **Makefile works**: `make --dry-run ci` shows expected commands
3. **Local test**: `cargo nextest run` works (if nextest installed)
</verification>

<success_criteria>
- [ ] .github/workflows/ci.yml exists with test, lint, coverage jobs
- [ ] CI triggers on push and PR to master/main
- [ ] cargo-nextest used for tests
- [ ] cargo-llvm-cov used for coverage
- [ ] Makefile has ci, test-local, coverage targets
- [ ] YAML syntax is valid
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
